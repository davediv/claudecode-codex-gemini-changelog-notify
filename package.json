{
	"name": "claudecode-codex-gemini-changelog-notify",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types",
		"test": "node --experimental-strip-types --test"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.59.2"
	}
}
